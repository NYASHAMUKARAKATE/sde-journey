// Variable declarations
var oldVariable = "owh is that it "; // Function-scoped
let mutableVar = "i am a Chameleon"; // Block-scoped
const PI = 3.14; // Immutable reference

// Reassigning values
oldVariable = "nyasha";
mutableVar = "mukarakate";

console.log(oldVariable);
console.log(mutableVar); 
console.log(PI);

// Attempting to reassign a const variable will throw an error